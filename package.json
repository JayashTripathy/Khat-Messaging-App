{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js && npm install cors",
    "heroku-postbuild": " npm install && cd public && npm install && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "heroku": "^7.60.2",
    "mongoose": "^6.3.3",
    "nodemon": "^2.0.16",
    "public": "^0.1.5",
    "socket.io": "^4.5.1"
  }
}
